# Dependencies (installed on Vercel)
node_modules

# Local / dev
.local
.replit
*.local

# Git
.git
.gitignore

# Docs (assets in attached_assets are needed by client build)
*.md
!README.md

# Tests and tooling (not needed in serverless runtime)
*.test.ts
*.spec.ts
__tests__
coverage
.env*.local
.env.development
